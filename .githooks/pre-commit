#!/bin/bash

# Auto-update cache-busting version in index.html when main.js is committed

if git diff --cached --name-only | grep -q '^main\.js$'; then
    TIMESTAMP=$(date +%s)
    sed -i '' "s/main\.js?v=[0-9]*/main.js?v=${TIMESTAMP}/" index.html
    git add index.html
fi
